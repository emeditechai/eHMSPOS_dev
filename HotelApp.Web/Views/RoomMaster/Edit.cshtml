@model HotelApp.Web.Models.Room
@{
    var isReadOnly = (bool?)(ViewBag.IsReadOnly) ?? false;
    var roomTypes = ViewBag.RoomTypes as IEnumerable<HotelApp.Web.Models.RoomType> ?? Enumerable.Empty<HotelApp.Web.Models.RoomType>();
    var floors = ViewBag.Floors as IEnumerable<HotelApp.Web.Models.Floor> ?? Enumerable.Empty<HotelApp.Web.Models.Floor>();
    var statuses = ViewBag.Statuses as IEnumerable<string> ?? Array.Empty<string>();
    ViewData["Title"] ??= isReadOnly ? "Room Details" : "Edit Room";
    var headingText = ViewData["Title"]?.ToString() ?? (isReadOnly ? "Room Details" : "Edit Room");
    string FormatDate(DateTime date) => date == default ? "Not recorded" : date.ToString("dd MMM yyyy HH:mm");
}

<style>
    .master-edit-container {
        background: white;
        border-radius: 12px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.08);
        padding: 30px 50px;
        max-width: 95%;
        width: 100%;
        margin: 0 auto;
    }

    .master-edit-header {
        display: flex;
        align-items: center;
        margin-bottom: 30px;
        padding-bottom: 20px;
        border-bottom: 2px solid #f0f0f0;
    }

    .master-edit-header h3 {
        margin: 0;
        color: var(--primary);
        font-size: 24px;
        font-weight: 600;
    }

    .audit-info {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 20px;
        background: #f8f9fa;
        padding: 20px;
        border-radius: 8px;
        margin-bottom: 25px;
    }

    .audit-item {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

    .audit-label {
        font-weight: 600;
        color: #666;
        font-size: 12px;
        text-transform: uppercase;
    }

    .audit-value {
        color: #333;
        font-size: 14px;
        font-weight: 500;
    }

    .master-form-section {
        margin-bottom: 25px;
    }

    .section-title {
        font-size: 16px;
        font-weight: 600;
        color: #333;
        margin-bottom: 15px;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .section-title i {
        color: var(--primary);
    }

    .master-form-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 20px 25px;
    }

    .master-form-group {
        display: flex;
        flex-direction: column;
    }

    .master-form-group label {
        font-weight: 500;
        margin-bottom: 8px;
        color: #333;
        font-size: 14px;
    }

    .master-form-group input,
    .master-form-group select,
    .master-form-group textarea {
        padding: 12px 15px;
        border: 1px solid #ddd;
        border-radius: 8px;
        font-size: 14px;
        transition: all 0.2s;
    }

    .master-form-group input:focus,
    .master-form-group select:focus,
    .master-form-group textarea:focus {
        outline: none;
        border-color: var(--primary);
        box-shadow: 0 0 0 3px rgba(26, 42, 108, 0.1);
    }

    .master-form-group input:read-only,
    .master-form-group select:disabled {
        background-color: #f5f5f5;
        cursor: not-allowed;
    }

    .master-form-group textarea {
        min-height: 80px;
        resize: vertical;
    }

    .master-form-group-full {
        grid-column: 1 / -1;
    }

    .master-form-group small {
        margin-top: 5px;
        font-size: 12px;
        color: #666;
    }

    .form-check {
        display: flex;
        align-items: center;
        gap: 8px;
        padding-top: 30px;
    }

    .form-check input {
        width: 18px;
        height: 18px;
        cursor: pointer;
    }

    .form-check label {
        margin: 0;
        cursor: pointer;
    }

    .master-form-actions {
        display: flex;
        gap: 15px;
        justify-content: flex-end;
        padding-top: 25px;
        border-top: 2px solid #f0f0f0;
    }

    .master-form-actions .btn-save,
    .master-form-actions .btn-cancel {
        padding: 12px 30px;
        font-size: 15px;
        font-weight: 600;
        border-radius: 8px;
        transition: all 0.2s;
    }

    .page-header {
        max-width: 95%;
        margin: 0 auto 20px;
    }

    @@media (max-width: 1400px) {
        .master-form-grid {
            grid-template-columns: repeat(2, 1fr);
        }
        .audit-info {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 768px) {
        .master-form-grid {
            grid-template-columns: 1fr;
        }
    }

    @@media screen and (min-width: 1200px) {
        .master-edit-container {
            max-width: 98%;
        }
    }

    @@media print {
        body {
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }
        @@page {
            size: landscape;
            margin: 0.5cm;
        }
        .master-edit-container {
            max-width: 100%;
            padding: 20px;
        }
    }
</style>

<div class="main-content">
    <div class="page-header">
        <h2><i class="fas fa-door-open"></i> @headingText</h2>
    </div>

    <div class="master-edit-container @(isReadOnly ? "form-readonly" : string.Empty)">
        <div class="master-edit-header">
            <h3><i class="fas fa-door-open"></i> @(isReadOnly ? $"Room {Model.RoomNumber} Overview" : $"Edit Room - {Model.RoomNumber}")</h3>
        </div>
        
        <form asp-action="Edit" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="CreatedDate" />
            <div asp-validation-summary="ModelOnly" class="validation-summary"></div>

            @if (Model != null && (Model.CreatedDate != default || Model.LastModifiedDate != default))
            {
                <div class="audit-info">
                    <div class="audit-item">
                        <span class="audit-label">Created</span>
                        <span class="audit-value">@FormatDate(Model.CreatedDate)</span>
                    </div>
                    <div class="audit-item">
                        <span class="audit-label">Last Updated</span>
                        <span class="audit-value">@FormatDate(Model.LastModifiedDate)</span>
                    </div>
                </div>
            }

            <div class="master-form-section">
                <div class="section-title">
                    <i class="fas fa-info-circle"></i>
                    Room Information
                </div>
                <div class="master-form-grid">
                    <div class="master-form-group">
                        <label asp-for="RoomTypeId">Room Rate <span class="text-danger">*</span></label>
                        <select asp-for="RoomTypeId" id="RoomTypeId" required disabled="@(isReadOnly ? "disabled" : null)">
                            <option value="">-- Select Room Rate --</option>
                            @{
                                var activeRates = ViewBag.ActiveRates as Dictionary<int, HotelApp.Web.Models.RateMaster>;
                                var roomCountByType = ViewBag.RoomCountByType as Dictionary<int, int>;
                            }
                            @if (roomTypes != null)
                            {
                                foreach (var roomType in roomTypes)
                                {
                                    var rate = activeRates?.ContainsKey(roomType.Id) == true ? activeRates[roomType.Id] : null;
                                    var rateInfo = rate != null ? $"₹{rate.BaseRate:N0}" : "No Rate";
                                    var created = roomCountByType?.ContainsKey(roomType.Id) == true ? roomCountByType[roomType.Id] : 0;
                                    var maxAvail = roomType.Max_RoomAvailability ?? 0;
                                    <option value="@roomType.Id"
                                            data-max="@maxAvail"
                                            data-created="@created">
                                        @roomType.TypeName — @rateInfo | Max: @maxAvail | Created: @created
                                    </option>
                                }
                            }
                        </select>
                        <span asp-validation-for="RoomTypeId" class="text-danger"></span>
                        <small class="form-text text-muted">Rates are managed in Rate Master. Showing current active rates.</small>
                        <div id="roomTypeInfo" style="display:none; margin-top:6px; padding:6px 12px; border-radius:6px; font-size:13px; font-weight:500;"></div>
                    </div>

                    <div class="master-form-group">
                        <label asp-for="RoomNumber">Room Number <span class="text-danger">*</span></label>
                        <input asp-for="RoomNumber" type="text" required readonly="@(isReadOnly ? "readonly" : null)" />
                        <span asp-validation-for="RoomNumber" class="text-danger"></span>
                    </div>
                    
                    <div class="master-form-group">
                        <label asp-for="Floor">Floor <span class="text-danger">*</span></label>
                        <select asp-for="Floor" required disabled="@(isReadOnly ? "disabled" : null)">
                            <option value="">-- Select Floor --</option>
                            @foreach (var floor in floors)
                            {
                                <option value="@floor.Id">@floor.FloorName</option>
                            }
                        </select>
                        <span asp-validation-for="Floor" class="text-danger"></span>
                    </div>
                    
                    <div class="master-form-group">
                        <label asp-for="Status">Status <span class="text-danger">*</span></label>
                        <select asp-for="Status" required disabled="@(isReadOnly ? "disabled" : null)">
                            <option value="">-- Select Status --</option>
                            @foreach (var status in statuses)
                            {
                                <option value="@status">@status</option>
                            }
                        </select>
                        <span asp-validation-for="Status" class="text-danger"></span>
                    </div>

                    <div class="master-form-group master-form-group-full">
                        <label asp-for="Notes">Notes</label>
                        <textarea asp-for="Notes" readonly="@(isReadOnly ? "readonly" : null)"></textarea>
                        <span asp-validation-for="Notes" class="text-danger"></span>
                    </div>

                    <div class="master-form-group">
                        <div class="form-check">
                            <input asp-for="IsActive" type="checkbox" disabled="@(isReadOnly ? "disabled" : null)" />
                            <label asp-for="IsActive">Is Active</label>
                        </div>
                    </div>
                </div>
            </div>

            @if (isReadOnly)
            {
                <div class="master-form-actions">
                    <a asp-action="List" class="btn-cancel">
                        <i class="fas fa-arrow-left"></i> Back to List
                    </a>
                </div>
            }
            else
            {
                <div class="master-form-actions">
                    <a asp-action="List" class="btn-cancel">
                        <i class="fas fa-times"></i> Cancel
                    </a>
                    <button type="submit" class="btn-save">
                        <i class="fas fa-save"></i> Update Room
                    </button>
                </div>
            }
        </form>
    </div>
</div>

<script>
    (function () {
        var sel = document.getElementById('RoomTypeId');
        var info = document.getElementById('roomTypeInfo');
        function showInfo() {
            var opt = sel.options[sel.selectedIndex];
            if (!sel.value) { info.style.display = 'none'; return; }
            var max = parseInt(opt.getAttribute('data-max')) || 0;
            var created = parseInt(opt.getAttribute('data-created')) || 0;
            var remaining = max - created;
            if (max > 0 && remaining <= 0) {
                info.style.display = 'block'; info.style.background = '#fee2e2'; info.style.color = '#b91c1c';
                info.innerHTML = '<i class="fas fa-exclamation-triangle"></i> Limit reached! Max: ' + max + ' | Created: ' + created;
            } else if (max > 0) {
                info.style.display = 'block'; info.style.background = '#ecfdf5'; info.style.color = '#065f46';
                info.innerHTML = '<i class="fas fa-check-circle"></i> Available: ' + remaining + ' of ' + max + ' | Created: ' + created;
            } else {
                info.style.display = 'block'; info.style.background = '#eff6ff'; info.style.color = '#1e40af';
                info.innerHTML = '<i class="fas fa-info-circle"></i> No max limit set | Created: ' + created;
            }
        }
        sel.addEventListener('change', showInfo);
        if (sel.value) showInfo();
    })();
</script>
